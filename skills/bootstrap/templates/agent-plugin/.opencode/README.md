# Opencode

Opencode reads `AGENTS.md` at the repository root as its primary instruction surface. This directory exists to mark the repo as opencode-aware for tools that look for a `.opencode/` presence indicator.

Skills installable via opencode live under `skills/` alongside the other agent-plugin surfaces.
